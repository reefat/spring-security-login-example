
CREATE TABLE USERS (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
  USERNAME VARCHAR(255),
  PASSWORD VARCHAR(255),
  PRIMARY KEY (ID)
);

ALTER TABLE USERS ADD CONSTRAINT USER_USER_NAME UNIQUE(USERNAME);
